## Agent skills

### 页面设计稿

`.superdesign/` 是本项目的页面设计稿目录。编写或修改页面前，必须先阅读 [设计系统](.superdesign/design-system.md)，并参考对应页面的设计稿。
实现时应遵循设计稿中的布局、视觉层级、颜色、字体、间距和交互状态；设计稿 HTML 仅作为视觉与交互参考，不应直接作为生产代码复制。

### 问题跟踪器

问题以本地 Markdown 文件的形式记录在 `.scratch/` 下。详见 `docs/agents/issue-tracker.md`。

### Triage 标签

本仓库使用默认的五个标准 triage 标签。详见 `docs/agents/triage-labels.md`。

### 领域文档

本仓库采用 single-context（单上下文）布局。详见 `docs/agents/domain.md`。
